#!/usr/bin/env bash
# Thin wrapper so `agent-pipeline` works from anywhere.
#   ln -s "$PWD/bin/agent-pipeline" /usr/local/bin/agent-pipeline
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "$HERE/cli/pipeline.py" "$@"
